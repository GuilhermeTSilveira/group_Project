package org.academiadecodigo.tropaDELETE;

import org.academiadecodigo.simplegraphics.graphics.Ellipse;
import org.academiadecodigo.simplegraphics.graphics.Rectangle;


public class Collison {

    private Game game;
    private Player player;

    /*
    public Collison(Game game, Player player) {
        this.game = game;
        this.player = player;
    }
    
        //AVATAR
       int rightTopX = (player.avatar.getX() + player.avatar.getWidth());
       int leftDown = (player.avatar.getY() + player.avatar.getHeight());
       int topX = player.avatar.getX();


       //OBJECT
       int objectRightTopX = (game.ellipse.getX() + game.ellipse.getWidth());
       int objectLeftTopX = game.ellipse.getX();
       int objectLeftTopY = game.ellipse.getY();



    public boolean isCollide(Rectangle avatar, Ellipse ellipse) {
        if (isLeftCollide()){
            return true;
        }
        return false;
    }

    private boolean isLeftCollide() {
        if (rightTopX >= objectLeftTopX && leftDown >= objectLeftTopY){
            return true;
        }
        return false;
    }


    //private boolean isRightCollide() {
      //  if ()
    //}
    */

}
